# Test environment Test Configuration

# API Configuration
base.url=https://jsonplaceholder.typicode.com
request.timeout=30000
connection.timeout=10000

# Performance Test Thresholds (in milliseconds)
# Test environment typically has higher thresholds due to shared resources
performance.threshold.users.api=1000
performance.threshold.posts.api=800
performance.threshold.comments.api=600